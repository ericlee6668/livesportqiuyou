<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingLeft="@dimen/dp6"
    android:paddingRight="@dimen/dp6">

    <TextView
        android:id="@+id/txtTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:paddingTop="@dimen/dp2"
        android:paddingBottom="@dimen/dp2"
        android:textColor="@color/black"
        app:layout_constrainedWidth="true"
        app:layout_constraintHorizontal_bias="0"
        app:layout_constraintHorizontal_chainStyle="packed"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/imgEvent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="上半场上半场11：59" />

    <ImageView
        android:id="@+id/imgEvent"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:layout_marginStart="@dimen/dp6"
        app:layout_constraintBottom_toBottomOf="@id/txtTime"
        app:layout_constraintLeft_toRightOf="@id/txtTime"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="@id/txtTime"
        tools:background="@color/live_gary"
        tools:src="@drawable/icon_yellow_card" />

    <TextView
        android:id="@+id/txtContent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/dp2"
        android:background="@drawable/shape_match_live_content"
        android:padding="@dimen/dp6"
        android:textColor="@color/black"
        app:layout_constraintLeft_toLeftOf="@id/txtTime"
        app:layout_constraintTop_toBottomOf="@id/txtTime"
        tools:text="上半场结束" />
</androidx.constraintlayout.widget.ConstraintLayout>